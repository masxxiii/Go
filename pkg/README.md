## Package

This directory stores libraries used in third-party applications. Other projects will import these libraries
expecting them to work. The `/pkg` directory is still a good way to explicitly communicate that the code in 
this directory is safe for use by others.